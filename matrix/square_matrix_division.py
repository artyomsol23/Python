def square_matrix_division(A, B):
    """
    Деление квадратных матриц A на B (эквивалентно умножению A на обратную матрицу B).
    Возвращает результирующую матрицу C = A × B^(-1).

    Параметры:
        A (list[list]): Исходная матрица (левая часть деления).
        B (list[list]): Квадратная матрица (правая часть деления).

    Возвращает:
        list[list]: Результат деления матриц (произведение A × B^(-1)).

    Исключения:
        ValueError: Если матрица B не квадратная или размерности матриц не согласованы.
    """
    # Проверка, что матрица B квадратная
    if len(B) != len(B[0]):
        raise ValueError("Матрица B должна быть квадратной!")
    
    # Проверка согласованности размерностей матриц A и B
    if len(A[0]) != len(B):
        raise ValueError("Число столбцов матрицы A должно равняться числу строк матрицы B!")
    
    # Вычисление обратной матрицы B
    B_inv = matrix_inverse(B)
    
    # Умножение матрицы A на обратную матрицу B
    return matrix_multiplication(A, B_inv)
