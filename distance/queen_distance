def queen_distance(a, b):
    dx = abs(a[0] - b[0])
    dy = abs(a[1] - b[1])
    return dx + dy if dx == 0 or dy == 0 or dx == dy else max(dx, dy)

# Пример:
start = (0, 0)
end = (3, 3)
print(queen_distance(start, end))  # 3
