import math

def euclidean_distance(a, b):
    return math.sqrt((a[0] - b[0]) ** 2 + (a[1] - b[1]) ** 2)

# Пример:
point_a = (0, 0)  # Старт
point_b = (3, 4)   # Конец
print(euclidean_distance(point_a, point_b))  # 5.0
